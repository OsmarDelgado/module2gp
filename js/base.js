// Ejercicios
/**
 * Pedir al usuario usuario un numero entre 1 y 10
 * Imprimir la tabla de multiplicar de ese número
 * p.ej. 
 * 3 x 1 = 3
 * 3 x 2 = 6
 */
// const num = parseInt( window.prompt( 'Give me a number between 1 and 10' ) );

// for( let i = 1; i <= 10; i++ ) {
//     console.log( `${ num } x ${ i } = ${ num * i }` );
// }

/**
 * Pedir al usuario una oracion
 * Imprimir un string con todas las vocales
 * Imprimir un string con todas las consonantes
 * hello world
 */
// const sentece = window.prompt( 'Give me a sentence' ).toLocaleLowerCase();

// let vowel = '';
// let consonant = '';

// for( let i = 0; i < sentece.length; i++ ) {
//     if( sentece[i] === 'a' || sentece[i] === 'e' || sentece[i] === 'i' || sentece[i] === 'o' || sentece[i] === 'u' ) {
//         vowel += ' ';
//         vowel += sentece[i];
//     } else {
//         consonant += ' ';
//         consonant += sentece[i];
//     }
    
// }

// console.log( `Vowel: ${ vowel }` );
// console.log( `Consonant: ${ consonant }` );

/**
 * Pedir el usuario un numero entre 10 y 100
 * Imprimir todos los numeros pares que existen entre 1 y ese numero
 */
// const num = parseInt( window.prompt( 'Give me a number between 10 and 100' ) );

// for( let i = 1; i <= num; i++ ) {
//     if( i % 2 === 0  ) {
//         console.log( i );
//     }
// }
